import '../loader.scss'
const Loader = () => {
    return (
        <div className='loader'>
            <img src="/infinite-spinner.svg" alt="Loading" />
        </div>
    )
}

export default Loader
